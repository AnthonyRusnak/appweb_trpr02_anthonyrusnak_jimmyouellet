export enum ActionType {
    ATTACK = 'attack',
    DEFEND = 'defend',
    SUMMON = 'summon',
    HEAL = 'heal',
    FLEE = 'flee'
}

export enum EventType {
    FLEE = 'flee',
    QUIT = 'quit',
    LOSS = 'loss',
    FIGHT_OVER = 'fightOver',
    WIN = 'win',
}